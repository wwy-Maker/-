# 《诸子百家·口诛笔伐》—— Unity 项目搭建指南（灰模验证阶段）

> **核心原则**：不导入任何外部图片资源，所有视觉元素全部使用 Unity 内置组件实现。

---

## 一、推荐的 Unity 项目目录结构

```
Assets/
├── Scripts/
│   ├── Core/               ← 全局核心系统
│   │   ├── GameManager.cs      (游戏状态管理、波次系统)
│   │   └── CameraController.cs (相机平滑跟随)
│   ├── Player/             ← 玩家相关
│   │   └── PlayerController.cs (移动、冲刺、朝向)
│   ├── Enemy/              ← 敌人基类 & 具体敌人
│   ├── Combat/             ← 战斗系统（弹幕、伤害计算、Buff）
│   ├── Flow/               ← 关卡流程（波次生成、房间管理）
│   ├── UI/                 ← UI 面板（血条、技能栏、暂停菜单）
│   ├── Economy/            ← 经济系统（经验、升级、流派选择）
│   └── Render/             ← 渲染相关（Shader、特效）
├── Scenes/                 ← 场景文件（.unity）
├── Prefabs/                ← 预制体（Player、Enemy 模板）
├── Art/
│   ├── Materials/          ← 灰模材质
│   ├── Sprites/            ← 程序化生成的 Sprite
│   └── Particles/          ← 粒子特效参数（后续阶段）
├── Audio/                  ← 音效（后续阶段）
├── Shaders/                ← 自定义 Shader
├── Configs/                ← ScriptableObject 配置表
├── Resources/              ← 动态加载资源
└── Settings/               ← 项目设置
```

---

## 二、在 Unity 编辑器中搭建"灰模玩家"

### 第一步：创建 Player GameObject

1. 在 Hierarchy 窗口，右键 → **Create Empty**
2. 将新建的 GameObject 重命名为 **Player**
3. 设置 Transform → Position 为 **(0, 0, 0)**

### 第二步：设为 Prefab（预制体）

1. 将 Hierarchy 中的 Player 拖入 `Assets/Prefabs/` 文件夹
2. 弹出窗口选择 **Original Prefab**
3. 此后所有对 Player 的修改都会自动同步到预制体

### 第三步：添加 SpriteRenderer（白色圆圈）

1. 选中 Player，Inspector 中点击 **Add Component** → 搜索 **SpriteRenderer**
2. 设置 SpriteRenderer 的 **Sprite**（二选一）：

   **方法 A（推荐，永远可用）：使用内置 Knob 精灵**
   - 点击 Sprite 字段右侧的 ◎ 圆点图标
   - 在弹出的窗口搜索 `Knob`
   - 选择 **Knob**（Unity 内置圆形精灵，无需安装任何包）
   - 如果搜不到 Knob，搜索 `Built-in`，展开 "Built-in Extra" 就能看到

   **方法 B：程序化创建 Circle 精灵资产**
   - 首先确保安装了 "2D Sprite" 包：Window → Package Manager → Unity Registry → 搜索 `2D Sprite` → Install
   - 然后 Project 窗口右键 → Create → Sprites → Circle
   - 把这个 Circle 拖入 SpriteRenderer 的 Sprite 字段

   > 注意：如果项目是用 3D 模板创建的，"2D Sprite" 包可能未安装，此时方法 B 的菜单项不会出现。推荐直接用方法 A，更快且零依赖。
3. 将 **Color** 设为纯白色 `(255, 255, 255, 255)`
4. 调整 Transform → Scale 为 **(0.5, 0.5, 1)**，让圆的大小合适

> **提示**：为了在灰模阶段能看清朝向，可以在 Player 下创建一个子物体：
> 右键 Player → Create Empty → 命名为 "Pointer"
> 给 Pointer 加 SpriteRenderer（Square），Color 设为浅灰，Scale 设为 (0.15, 0.06, 1)，Position 设为 (0.3, 0, 0)
> 这样圆形 + 小矩形 = 像一只"眼睛"，能直观看到玩家朝向。

### 第四步：添加 Rigidbody2D（物理组件）

1. **Add Component** → 搜索 **Rigidbody2D**
2. 配置如下：
   - **Body Type**: Dynamic
   - **Gravity Scale**: `0`（俯视角 Roguelike，不需要重力）
   - **Linear Drag**: `5`（松开按键后有适度阻尼，不会一直滑行）
   - **Constraints** → Freeze Rotation: ✅ 勾选 Z（防止碰撞导致旋转）

### 第五步：添加 CircleCollider2D（碰撞体）

1. **Add Component** → 搜索 **CircleCollider2D**
2. 设置 **Radius** 为 `0.5`（与 SpriteRenderer 视觉大小匹配）
3. **Is Trigger**: ✅ 勾选（弹幕类游戏通常用 Trigger 判定命中，而非物理碰撞）

### 第六步：挂载 PlayerController.cs

1. **Add Component** → 搜索 **PlayerController**
2. 参数说明：
   | 参数 | 默认值 | 说明 |
   |------|--------|------|
   | Move Speed | 6 | 基础移动速度 |
   | Move Damping | 0.2 | 惯性系数（越小越灵敏） |
   | Dash Speed Multiplier | 2.5 | 冲刺速度倍率 |
   | Dash Duration | 0.15 | 冲刺持续时间（秒） |
   | Dash Cooldown | 1.5 | 冲刺冷却时间（秒） |
   | Clamp To Room | ✓ | 是否限制在房间内 |

### 第七步：设置 Tag 为 Player

1. 在 Inspector 最顶部，点击 **Tag** 下拉框
2. 选择 **Player**（如果没有，点击 **Add Tag...** → 添加名为 `Player` 的 Tag）
3. 这一步很重要：CameraController 通过 Tag 自动查找玩家

---

## 三、搭建 Main Camera（相机跟随）

1. 选中 Hierarchy 中的 **Main Camera**
2. 设置 Transform → Position 为 **(0, 0, -10)**（2D 游戏相机 Z 轴通常为 -10）
3. 设置 Camera 组件：
   - **Projection**: Orthographic（正交投影）
   - **Size**: `7`（视野范围，值越大场景看起来越小）
   - **Background**: 纯黑色 `(0, 0, 0)` 或深灰
4. **Add Component** → 搜索 **CameraController**
5. 参数说明：
   | 参数 | 默认值 | 说明 |
   |------|--------|------|
   | Target | 拖入 Player | 跟随目标（留空则自动查找 Tag="Player"） |
   | Offset | (0, 0, -10) | 相机偏移 |
   | Smooth Time | 0.15 | 平滑时间（秒） |
   | Max Speed | 50 | 最大移动速度 |

---

## 四、搭建 GameManager（全局管理器）

1. Hierarchy 中右键 → **Create Empty**，命名为 **GameManager**
2. **Add Component** → 搜索 **GameManager**
3. 参数说明：
   | 参数 | 默认值 | 说明 |
   |------|--------|------|
   | Room Size | (20, 14) | 战斗房间尺寸 |
   | Wave Prepare Time | 3 | 波次间准备时间（秒） |
   | Base Enemies Per Wave | 5 | 第 1 波敌人数量 |
   | Extra Enemies Per Wave | 2 | 每波递增敌人数量 |

---

## 五、搭建战斗房间（灰模地板 + 墙壁）

### 地板

1. Hierarchy 中右键 → **2D Object** → **Sprites** → **Square**
2. 重命名为 **Floor**
3. Transform → Scale 设为 **(20, 14, 1)**（与 GameManager 的 roomSize 一致）
4. SpriteRenderer → **Color** 设为深灰色 `(40, 40, 40, 255)`
5. Order in Layer 设为 `-1`（确保地板在所有对象下面）

### 墙壁（四面）

创建 4 个细长 Square 围住地板：

| 墙壁 | Position | Scale | 说明 |
|------|----------|-------|------|
| Wall_Top | (0, 7.15, 0) | (20.3, 0.3, 1) | 上墙 |
| Wall_Bottom | (0, -7.15, 0) | (20.3, 0.3, 1) | 下墙 |
| Wall_Left | (-10.15, 0, 0) | (0.3, 14.3, 1) | 左墙 |
| Wall_Right | (10.15, 0, 0) | (0.3, 14.3, 1) | 右墙 |

每面墙的配置：
- SpriteRenderer → Color: `(120, 120, 120, 255)`（浅灰）
- Add Component → **BoxCollider2D**（防止玩家穿墙）

> 建议：创建空的 **Walls** 父节点，将四面墙拖入其下，保持 Hierarchy 整洁。

---

## 六、最终场景 Hierarchy 结构预览

```
SampleScene
├── Main Camera              ← CameraController.cs
├── GameManager              ← GameManager.cs
├── Floor                    ← Square, 深灰, (20,14,1)
├── Walls (空父节点)
│   ├── Wall_Top             ← Square, 浅灰, + BoxCollider2D
│   ├── Wall_Bottom
│   ├── Wall_Left
│   └── Wall_Right
└── Player                   ← Tag="Player", PlayerController.cs
    ├── SpriteRenderer       (Knob/Circle, 白色)
    ├── Rigidbody2D          (Gravity=0, Drag=5, FreezeZ)
    ├── CircleCollider2D     (Radius=0.5, IsTrigger)
    └── Pointer (子物体)     (Square, 浅灰, 指示朝向)
```

---

## 七、验证步骤

运行游戏后，依次验证：

| 序号 | 验证项 | 预期行为 |
|------|--------|----------|
| 1 | WASD 移动 | 白色圆圈平滑移动，松手后有轻微惯性 |
| 2 | 鼠标朝向 | Player 始终面朝鼠标光标方向 |
| 3 | 空格冲刺 | 朝移动方向（或无输入时朝鼠标方向）快速冲刺，有冷却 |
| 4 | 相机跟随 | 镜头平滑跟随 Player，无抖动 |
| 5 | 房间限制 | Player 无法移出墙壁范围 |
| 6 | ESC 暂停 | 按 ESC 暂停/继续，暂停时所有移动冻结 |
| 7 | Console 日志 | 查看 GameManager 的状态日志输出 |

如果以上全部正常，灰模阶段的基础架构即搭建完成，可以进入后续开发。
